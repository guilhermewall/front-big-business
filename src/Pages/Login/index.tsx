import { StyledDiv } from "./styles";
import Form from "../../Components/Form";
import UserInteraction from "../../Components/UserInteractionForm";

const Login = () => {
  return (
    <StyledDiv>
      <UserInteraction />
      <Form />
    </StyledDiv>
  );
};

export default Login;
